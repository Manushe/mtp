#include <stdio.h>
int main(){
    float ladoquadrado,area;
    printf("Meu benzinho, informe o lado do quadrado: ");
    scanf("%f",&ladoquadrado);
    area= ladoquadrado*ladoquadrado;
    printf("A area do quadrado e: %f",area);

return 0;}
